{% extends 'post/layout.html.twig' %}

{% block content %}
    <h1>Post creation</h1>

    <div class="form">
        {{ form_start(form, {attr: {novalidate: 'novalidtae'}}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}
    </div>

    <ul>
        <li>
            <a href="{{ path('post_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
